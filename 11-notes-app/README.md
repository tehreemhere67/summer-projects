# Notes App
A Flask web app with user authentication. Register, login, and manage personal notes. Passwords are hashed with bcrypt, sessions handle login state, and notes are stored in SQLite tied to each user account.
